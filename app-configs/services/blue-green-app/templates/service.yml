apiVersion: v1
kind: Service
metadata:
  name: rollout-bluegreen-active
spec:
  type: NodePort
  selector:
    app: trivial-go-web-app
  ports:
      - nodePort: {{ .Values.service.nodePort }}
        protocol: TCP
        port: 8080
